[Unit]
Description=Image Converter
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/image-converter
ExecStart=/usr/local/bin/image-converter
Restart=always
AmbientCapabilities=CAP_NET_BIND_SERVICE

[Install]
WantedBy=multi-user.target
